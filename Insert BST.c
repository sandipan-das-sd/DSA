#include<stdio.h>
#include<stdlib.h>
struct node
{
	int data;
	struct node*llink;
	struct node*rlink;
}
struct node*newNode(int data)
{
	struct node *n=(struct *node)malloc(sizeof(struct node));
	n->
}
